# Serverless build. Deliberately NOT "-r requirements.txt".
#
# yfinance is omitted: it drags curl_cffi, websockets and protobuf for what amounts to an
# HTTP GET, ~45MB of a bundle that has a hard size ceiling. Stooq needs only urllib and
# pandas, so it becomes the sole source here — fetch() already treats a missing yfinance as
# a failed source and falls through, so this needs no code change.
#
# imageio-ffmpeg is added because serverless runtimes ship no ffmpeg binary. It is the
# single largest item at 77MB and is not optional if you want to encode video.
flask>=3.0
matplotlib>=3.8
numpy>=1.26
pandas>=2.0
imageio-ffmpeg>=0.4.9
